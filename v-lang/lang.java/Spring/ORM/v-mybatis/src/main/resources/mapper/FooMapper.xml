<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.mapper.FooMapper">

    <insert id="insert" parameterType="Foo" useGeneratedKeys="true" keyProperty="id">
        insert into t_foo (name) values (#{name})
    </insert>

</mapper>
